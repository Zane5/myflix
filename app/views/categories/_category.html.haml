%article.video_category
  %header
    %h1= link_to category.name, category
  .video.row
    - category.videos.each do |video|
      .video.col-sm-2
        =link_to video do
          =image_tag video.cover_small_url
